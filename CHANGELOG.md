# Changelog

## 0.1.0

Initial release: decorates the built-in markdown preview's front-matter
table (`visibility` row in its value's colour, `reveal_when` dimmed),
colours the `GM notes` heading, and tints everything below it.
