---
visibility: gm-only
canon: true
status: active
summary: A sample gm-only entity for manual verification.
reveal_when: the coronation
aliases:
  - first alias
  - second alias
---

# Sample gm-only entity

Public-looking paragraph (but the whole file is gm-only).

---

## GM notes

This paragraph and everything below it should carry the tint and bar.

A second paragraph, also tinted.

- A bulleted list item. Its marker should sit in its own indent, not
  crammed against the left bar.
  - A nested item, indented one further level.
- A second top-level item.

```
A fenced code block. Its background should still show the tint and bar,
with the code's own monospace styling on top.
```

> A blockquote. It should carry the tint and bar like any other block,
> with its own quote-marker styling still visible.

### A nested heading

This heading and this trailing paragraph should still carry the tint and
bar — the separator's effect is not limited to top-level paragraphs.
